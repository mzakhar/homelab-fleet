apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clean-mail-data
  namespace: clean-mail
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: clean-mail-backend
  namespace: clean-mail
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clean-mail-backend
  namespace: clean-mail
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clean-mail-backend
  template:
    metadata:
      labels:
        app: clean-mail-backend
    spec:
      imagePullSecrets:
        - name: clean-mail-ghcr-pull
      serviceAccountName: clean-mail-backend
      containers:
        - name: backend
          image: ghcr.io/mzakhar/clean-mail-backend@sha256:e21e4453c9b085d9af35acb8ffb14bea72045704e0313f4ca6d29e52d5badd36
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: clean-mail-secrets
          env:
            - name: SERVER_VERDICT_PROJECTION_ENABLED
              value: "true"
            - name: TRIAGE_INLINE_CLASSIFICATION_ENABLED
              value: "false"
            # Workload Identity Federation for Anthropic API.
            # IDs are non-secret; issuer "k3s-home" registered with inline JWKS.
            - name: ANTHROPIC_FEDERATION_RULE_ID
              value: "fdrl_013QYr2tJBojGFtgHqixHaH7"
            - name: ANTHROPIC_ORGANIZATION_ID
              value: "b876c729-18bc-4732-ba14-e5f964567a77"
            - name: ANTHROPIC_SERVICE_ACCOUNT_ID
              value: "svac_01D9WDqwhVfuov9wYXZZrPEn"
            - name: ANTHROPIC_WORKSPACE_ID
              value: "wrkspc_011HFuRjHxzjA4tcweZbTjGX"
            - name: ANTHROPIC_IDENTITY_TOKEN_FILE
              value: /var/run/secrets/anthropic/token
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.observability:4318
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: http/protobuf
            - name: OTEL_SERVICE_NAME
              value: clean-mail-backend
            - name: OTEL_TRACES_SAMPLER
              value: parentbased_always_on
            # Emit stable HTTP semantic conventions alongside the legacy ones so
            # this app shares label names with vs-book-app. Rules, Homepage
            # cards, and the Services RED dashboard target the stable names.
            # ponytail: http/dup doubles HTTP server metric series (12 -> 24
            # here) to avoid a blind window during the switch. Change to "http"
            # once nothing queries http_server_duration_milliseconds_* and the
            # 30 day legacy history is no longer wanted.
            - name: OTEL_SEMCONV_STABILITY_OPT_IN
              value: http/dup
          volumeMounts:
            - name: data
              mountPath: /data
            - name: anthropic-identity
              mountPath: /var/run/secrets/anthropic
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
              httpHeaders:
                # kubelet probes use the pod IP as Host, which TrustedHostMiddleware
                # would reject; pin an allowlisted host for the probe request.
                - name: Host
                  value: localhost
            initialDelaySeconds: 10
            periodSeconds: 15
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: clean-mail-data
        - name: anthropic-identity
          projected:
            sources:
              - serviceAccountToken:
                  path: token
                  audience: anthropic  # must match the audience configured on the federation rule
                  # Short-lived: kubelet rotates at ~80% of expiry, so a fresh
                  # jti is available every ~8 min. The cluster's WIF issuer
                  # enforces single-use jti; combined with the process-wide
                  # cached client (core/llm.py) re-exchanges practically always
                  # see a new projected token.
                  expirationSeconds: 600
---
apiVersion: v1
kind: Service
metadata:
  name: clean-mail-backend-svc
  namespace: clean-mail
spec:
  selector:
    app: clean-mail-backend
  ports:
    - port: 8000
      targetPort: 8000
