apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system/gotk-components.yaml
  - flux-system/gotk-sync.yaml
  - apps/vs-book-app.yaml
  - apps/gmail-frontend.yaml
  # P6 flip (single reconciliation): uncomment apps/clean-mail AND remove
  # apps/gmail-frontend.yaml above in the same commit. Gated per
  # specs/Deploy Authority Migration - Clean Mail.md — old gmail-app stack is
  # the rollback ref until soak completes.
  # - apps/clean-mail
  - apps/synth.yaml
  - platform/action-runner
  - platform/observability
  - platform/homepage
  - platform/homeassistant
  - platform/cloudflared
  - sites/zakharhome
